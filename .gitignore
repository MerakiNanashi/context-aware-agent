# Dependency directories (never commit these)
node_modules/
jspm_packages/
web_modules/

# Local configuration and environment secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript build output compiler artifacts
dist/
build/
out/
tsconfig.tsbuildinfo

# Testing, coverage, and snapshot reports
coverage/
*.lcov
.nyc_output
.jest/

# Package manager lockfile considerations
# (Keep package-lock.json, but you can ignore log footprints)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pid

# Operating System specific files
.DS_Store
Thumbs.db